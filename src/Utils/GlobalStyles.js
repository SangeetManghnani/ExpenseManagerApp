import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    text : {
        color: 'white',
        fontSize: 12,
    },
    textMediumBold : {
        color: 'white',
        fontSize: 12,
        fontWeight: 'bold'
    },
    textHeading : {
        color: 'white',
        fontSize: 16,
        color: 'white'
    },
    textHeadingBold: {
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold',
    },
    textExtraLarge: {
        fontSize: 24,
        color: 'white',
        fontWeight: 'bold'
    }
});

export default globalStyles;